extends layout

block content
  .row
    .col-md-6
      h1 Edit a movie
      form(role="form", method="post", action="/movies/#{movie.id}?_method=put")
        include editform
        button.btn.btn-primary(type="submit") Edit movie

